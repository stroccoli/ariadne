# Production dependencies — mirrors [project.dependencies] in pyproject.toml.
# Offline extras (evals, ingestion) are NOT listed here; install them with:
#   pip install -e ".[evals]"
#   pip install -e ".[ingestion]"
fastapi>=0.115.0
google-genai>=1.0.0
langgraph>=0.2.0
pydantic>=2.7.0
python-dotenv>=1.0.1
qdrant-client>=1.9.1
sentry-sdk[fastapi]>=2.0.0
slowapi>=0.1.9
tenacity>=8.2.0
uvicorn[standard]>=0.32.0